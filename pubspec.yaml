name: mm_thai_bus_guide
description: Your Bus Guide for Myanmar and Thailand.
version: 1.0.0+1
environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  shared_preferences: ^2.2.2
  flutter_localizations:
    sdk: flutter
  intl: ^0.18.1
  url_launcher: ^6.2.6
  flutter_tts: ^3.8.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/
  generate: true
  # App icon/splash via command: flutter pub run flutter_launcher_ico
ns
